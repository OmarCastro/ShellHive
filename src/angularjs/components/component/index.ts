import connector = require("./component.directive")
import port = require("./port.directive")
import inputPort = require("./input-port.directive")
import outputPort = require("./output-port.directive")
export const imports = [connector, port, inputPort, outputPort]