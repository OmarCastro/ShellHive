import graph = require("./graph.directive")
import graphCttrl = require("./graph.controller")
import elementScope = require("./element-scope.directive")
export const imports = [graph, graphCttrl, elementScope]