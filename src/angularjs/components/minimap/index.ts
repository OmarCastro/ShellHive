import minimap = require("./minimap.directive")
import miniComponent = require("./mini-component.directive")
import miniConnector = require("./mini-connector-directive")
export const imports = [minimap, miniComponent, miniConnector]